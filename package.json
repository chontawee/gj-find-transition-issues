{
  "name": "@bloobirds-it/action-transition-multiple-jira-issues",
  "version": "1.0.4",
  "description": "Transition multiple jira issues from pull request",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "ncc build index.js --minify",
    "watch": "ncc build index.js --watch --minify"
  },
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/github": "^4.0.0",
    "axios": "^0.21.1"
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "@vercel/ncc": "^0.27.0"
  }
}
